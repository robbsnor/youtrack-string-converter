image: node:latest


before_script:
  - node -v
  - npm -v
  - apt-get update -qq && apt-get install -y -qq
  - npm install


stages:
  - prep
  # - build
  # - deploy


prep:
  stage: prep
  script:
    - ls
    # - npm install
  cache:
    paths:
      - node_modules
  only:
    - master


build:
  stage: build
  script:
    - npm run prod
  artifacts:
    paths:
      - dist
  only:
    - master


staging:
  stage: deploy
  script:
    - cd dist
    - ls
  only: 
    - master